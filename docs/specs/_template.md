# Spec: (nome da feature)

Status: `SPEC_DRAFT` | `SPEC_READY`  
TASK-ID:  
Data:

## Context

O que existe hoje no repo (rotas live vs `modules/` comentados). Sem inventar.

## Goal

Uma frase.

## User Story

Como [papel], quero [ação], para [benefício].

## Requirements

- [ ] …

## Business Rules

Só regras confirmadas (código existente ou decisão do humano). Se faltar, pergunta — não preenchas com exemplos de outros produtos.

## Acceptance Criteria

Testáveis (método, path, status, body, header, ecrã).

- [ ] …

## Edge Cases

- …

## Non-Goals

O que esta spec **não** faz.

## Testing Expectations

O que corre neste lab: `tsc`, vitest do recorte se existir, k6 se carga. Se a spec tocar UI: Playwright (`pnpm test:e2e`) — cenário da jornada.
