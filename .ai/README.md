# .ai

Workflows e evidência. Agentes: `.cursor/agents/`.

- `workflows/` — `/start-task` (`playwright.md` se UI, `database.md` se schema/seed)
- `runs/_template/` → `runs/<TASK-ID>/`
- Specs: `docs/specs/`
