# Review

- Sem Nest/Next; ORM só em `@repositories/prisma/`.
- Controllers Zod; erros via handler; envelope `{ session }`.
- Skills/rules actualizados (sem Drizzle).
- 🟡 `venueExists` no `SessionsRepository` mistura venue; aceitável até haver agregado venues.
- Sem push.
