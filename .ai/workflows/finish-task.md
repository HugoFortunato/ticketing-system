# Finish-task

Reviewer OK.

1. `state.md`: `READY_TO_MERGE`.
2. `review.md` (e `qa-report.md` se o QA correu).
3. Resume: spec, o que mudou, o que não se fez.
4. Sem `git push`. Commit só se o humano pedir.
